# GPU Benchmarking Requirements
# 
# IMPORTANT: This file is for reference only!
# 
# DO NOT use 'pip install -r requirements.txt' as it will install CPU-only PyTorch.
#
# Installation Instructions:
# -------------------------
# 
# Method 1 (Recommended - with uv):
#   uv sync
#   uv run main.py
#
# Method 2 (Manual - with pip):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#   pip install matplotlib numpy psutil transformers
#
# The pyproject.toml file is configured to install CUDA-enabled PyTorch automatically.

matplotlib>=3.10.7
numpy>=2.3.5
psutil>=7.1.3
transformers>=4.57.1

# PyTorch with CUDA 12.1 support (install from PyTorch index):
# torch>=2.5.1
# torchvision>=0.20.1
# torchaudio>=2.5.1
# Install via: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
